/**
 * Constants Index
 * Central export point for all application constants
 */

export { STRINGS, type StringKeys } from './strings';
import { STRINGS } from './strings';

// Re-export for easier imports
export const {
  UI,
  API_KEYS,
  WIDGETS,
  STOCK_CHART,
  FINANCE_CARD,
  DASHBOARD,
  NAVIGATION,
  LOADING,
  ERRORS,
  SUCCESS,
  VALIDATION,
  STOCK,
} = STRINGS;
